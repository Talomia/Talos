/**
 * @deprecated Use `~/components/ui/ErrorBoundary` directly instead.
 * This re-export exists only for backward compatibility.
 */
export { ErrorBoundary as default } from '~/components/ui/ErrorBoundary';
